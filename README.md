# ParallaxSensitivityScript

In the div you want to add parallax write `story  = "True"` and `s=(Value of your choice)`  for scale sensitivity and  `m=(Value of your choice)` for movement sensitivity..
